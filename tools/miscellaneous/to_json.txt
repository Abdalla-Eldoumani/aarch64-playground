However, not all data types are passed through the general-purpose x or w registers. Some data types, such as floating-point values, use dedicated floating-point and SIMD registers instead. For example, printing a float or double value requires passing the value through the appropriate s or d register. Make sure that values are stored and passed using the correct register type that matches the corresponding format specifier, as using the wrong register can cause printf unintended behaviours.