# Rust
emulator/target/
emulator/Cargo.lock

# Node
node_modules/
web/node_modules/
web/.next/
web/out/
web/tsconfig.tsbuildinfo
# Root package-lock.json is generated by the Vercel install step (so the
# root `next` declaration gets installed for framework detection). It's not
# the source of truth for web/ -- that lives in web/package-lock.json.
/package-lock.json

# WASM build output
web/lib/wasm/

# Secrets
.env
.env.local
.env.*.local

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test artifacts
.playwright-mcp/

# Editors
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
