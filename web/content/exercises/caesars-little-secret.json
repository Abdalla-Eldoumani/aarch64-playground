{
  "title": "Caesar's little secret",
  "slug": "caesars-little-secret",
  "order": 20,
  "topic": "strings",
  "difficulty": "challenge",
  "prompt": "The string in `.data` was encrypted the way Julius Caesar liked it: every letter shifted three places forward, wrapping around the end of the alphabet. Decode it in place and print the result.\n\nWalk the bytes with `ldrb`. For each lowercase letter, shift back by three; if that falls below `'a'`, add 26 to wrap around. Store the decoded byte back with `strb` and leave the spaces alone. The printf at the bottom prints the string you repaired.\n\n## what is checked\n\n- the decoded message is printed\n- the program exits cleanly\n- the decode happens byte by byte in memory (`ldrb` and `strb`, no pasting the answer into `.data`)",
  "starter": "// decode a message caesar-shifted three letters forward\ndefine(fp, x29)\ndefine(lr, x30)\n\ndefine(ptr_r, x19)\ndefine(ch_r, w20)\n\n        .data\nsecret:     .string \"dvvhpeob lv ixq\"\nfmt:        .string \"%s\\n\"\n\n        .text\n        .balign 4\n        .global main\nmain:\n        stp     fp, lr, [sp, -16]!\n        mov     fp, sp\n\n        ldr     ptr_r, =secret\n\n        // TODO: walk the string byte by byte. Shift every lowercase\n        // letter back by three, wrapping so that 'a' goes around to\n        // 'x' (add 26 whenever the shift falls below 'a'). Store each\n        // decoded byte back where it came from with strb, and leave\n        // spaces and the terminator alone.\n\n        ldr     x0, =fmt\n        ldr     x1, =secret                 // now decoded in place\n        bl      printf\n\n        mov     w0, 0\n        ldp     fp, lr, [sp], 16\n        ret\n",
  "args": "",
  "variant": "write",
  "acceptance": {
    "results": [
      {
        "kind": "stdout",
        "equals": "assembly is fun\n"
      },
      {
        "kind": "exit",
        "equals": 0
      }
    ],
    "structural": [
      {
        "kind": "uses-instruction",
        "mnemonic": "ldrb"
      },
      {
        "kind": "uses-instruction",
        "mnemonic": "strb"
      },
      {
        "kind": "forbids-literal",
        "value": "assembly"
      }
    ]
  }
}
